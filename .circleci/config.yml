version: 2
jobs:
  Examples:
    docker:
      - image: microsoft/aspnetcore-build:2.0
    steps:
      - checkout
      - run: find .
      - run: build examples
  Source:
    docker:
      - image: microsoft/aspnetcore-build:2.0
    steps:
      - checkout
      - run: find .
      - run: build src
workflows:
  version: 2
  one_and_two:
    jobs:
      - Examples
      - Source